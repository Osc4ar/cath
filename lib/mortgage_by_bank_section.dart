import 'package:flutter/material.dart';

class MortgageByBank extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Color(0xFF182C47),
      child: Text('BANCOS'),
    );
  }
}